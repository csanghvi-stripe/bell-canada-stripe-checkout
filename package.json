{
  "name": "bell-canada-checkout",
  "version": "1.0.0",
  "description": "Bell Canada checkout page with Stripe integration",
  "main": "server-local.js",
  "scripts": {
    "start": "node server-local.js",
    "dev": "node server-local.js"
  },
  "dependencies": {
    "stripe": "^12.0.0",
    "dotenv": "^10.0.0"
  },
  "engines": {
    "node": ">=14"
  }
}
